/**
 * Web stub for Android widget task registration.
 *
 * `react-native-android-widget` is native-only; nothing to register on web.
 */
export function registerWidgetTask(): void {
  // no-op on web
}
