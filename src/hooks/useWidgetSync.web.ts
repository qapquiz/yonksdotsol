/**
 * Web stub for `useWidgetSync`.
 *
 * Android home-screen widgets don't exist on web; the real hook (and its
 * native imports — react-native-android-widget, expo-task-manager,
 * expo-background-fetch) never loads on the web platform.
 */
export function useWidgetSync(): void {
  // no-op on web
}
