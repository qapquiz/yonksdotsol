{
"id": "42620c4c",
"title": "Clean up dead code: delete unused hooks, tests, and cache invalidation",
"tags": [
"cleanup",
"dead-code"
],
"status": "closed",
"created_at": "2026-04-26T17:47:16.303Z"
}

After finishing RFC-01 and RFC-02 properly, several files become dead code.

**Delete these hooks** (nothing imports them after usePositionsPage replaces them):

- `src/hooks/positions/usePositionFetch.ts`
- `src/hooks/positions/useBatchTokenData.ts`

**Delete these tests**:

- `src/__tests__/hooks/usePositionFetch.test.ts`
- `src/__tests__/hooks/useBatchTokenData.test.ts` ( DataService tests move to their own file)

**Move DataService tests** from `useBatchTokenData.test.ts` into a proper `src/__tests__/services/data.test.ts`

**Fix usePositionsPage cache invalidation**:

- Add `CacheManager.getInstance().invalidatePattern('initial_deposits:')` on wallet change (was in old usePositionFetch)
- Add `CacheManager.getInstance().delete(getUpnlPerPositionKey(walletAddress))` on refresh (was in old usePositionFetch)
- Fix the throttled refresh — `lastRefreshRef` is set but never checked before allowing refresh
