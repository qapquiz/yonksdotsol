{
"id": "047daa20",
"title": "Deepen App Screen God Component (app/index.tsx)",
"tags": [
"architecture",
"deepening"
],
"status": "closed",
"created_at": "2026-04-24T19:12:18.445Z"
}

## Completed — God Component Deepening

### Extracted Two Hooks

**`usePositionFetch(walletAddress, options?)`** (`src/hooks/positions/usePositionFetch.ts`, 90 lines)

- Handles DLMM position fetching, cache invalidation on wallet change, throttled refresh
- Accepts `fetchFn` option for testability (DI seam)
- Returns `{ positions, isLoading, refresh }`

**`useWalletLifecycle()`** (`src/hooks/useWalletLifecycle.ts`, 76 lines)

- Wraps `useMobileWallet()` with 500ms timeout fallback
- Manages connect/disconnect lifecycle
- Returns `{ walletReady, walletAddress, isConnecting, handleConnect, handleDisconnect }`

### Refactored Component

- `app/index.tsx`: 198 → 92 lines (pure UI shell — theme, header, PositionsList)
- All orchestration logic extracted into hooks
- Cache invalidation stays coupled to position fetch lifecycle (not split)

### Tests

- 14 new tests in `src/__tests__/hooks/usePositionFetch.test.ts`
- Tests cover: fetch logic, throttle, wallet change detection, Map operations
- Full suite: 175 tests passing across 7 files
- tsc clean, build passes

### Remaining TODOs

- `useWalletLifecycle` tests (timeout fallback behavior) — can be added when wallet mocking is set up
