{
"id": "8c8fd287",
"title": "Finish RFC-01: Delete old cached wrappers, make DataService the only path",
"tags": [
"rfc-01",
"cleanup"
],
"status": "closed",
"created_at": "2026-04-26T17:47:16.303Z"
}

Old cached wrappers still exist alongside the new DataService, creating two parallel paths to cached token data.

- Delete `fetchTokenPriceData` from `src/tokens/index.ts` (the cached wrapper using `CacheManager.getInstance()` directly)
- Delete `fetchPoolPriceAtTimestamp` from `src/utils/positions/meteora-ohlcv.ts` (cached wrapper)
- Delete `fetchHistoricalSOLPrice` from `src/utils/positions/pyth-benchmarks.ts` (cached wrapper)
- Delete `src/utils/cache/cacheKeys.ts` (now internalized in DataService)
- Verify nothing imports these deleted exports (they're currently unused)
- Keep pure fetch functions: `fetchTokenFromRpc`, `fetchOHLCVPriceAtTimestamp`, `fetchHistoricalSOLPriceFromApi`
- Keep DataService as the sole entry point for cached data access
