{
"id": "237da0cb",
"title": "Deepen Position Data Pipeline (PositionCard + page orchestration)",
"tags": [
"architecture",
"deepening"
],
"status": "closed",
"created_at": "2026-04-24T19:12:18.445Z"
}

## Completed — Position Data Pipeline Deepening

### Created `computePositionViewData` pure function

- `src/utils/positions/computePositionViewData.ts` (124 lines)
- Takes `ComputePositionViewDataInput` → returns `PositionViewModel`
- All 8 computation blocks consolidated into a single pure function
- No React, no store access, fully testable

### Refactored PositionCard

- `src/components/positions/PositionCard.tsx`: 155 → 90 lines
- Now only has 3 useMemo: `pnlSelector`, `activeId`, `vm` (single computePositionViewData call)
- Pure rendering layer — no business logic

### Tests

- 16 new tests in `src/__tests__/utils/computePositionViewData.test.ts`
- Covers: null positionData, null tokens, partial tokens, in/out of range, fees, PnL, liquidity shape, current price
- Full suite: 191 tests passing across 8 files
- tsc clean, lint clean, build passes
