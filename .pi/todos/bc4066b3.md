{
  "id": "bc4066b3",
  "title": "Deepen Data Fetching Layer (tokens + CacheManager + env + price APIs)",
  "tags": [
    "architecture",
    "deepening"
  ],
  "status": "closed",
  "created_at": "2026-04-24T19:12:18.445Z"
}

## Completed — Data Fetching Layer Deepening

### Extracted pure fetch functions (testable without CacheManager)

**tokens/index.ts**:
- Exported `fetchTokenFromRpc(mint)` (was private) — pure RPC call + parse
- `fetchTokenPriceData` remains as thin caching wrapper using `getOrFetch`

**meteora-ohlcv.ts**:
- Extracted `fetchOHLCVPriceAtTimestamp(poolAddress, timestamp, timeframe)` — pure API call + candle matching
- `fetchPoolPriceAtTimestamp` now delegates to the pure function and caches non-null results
- Extracted constants: `METEORA_OHLCV_BASE_URL`, `MAX_TIME_DIFF`

**pyth-benchmarks.ts**:
- Extracted `fetchHistoricalSOLPriceFromApi(timestamp)` — pure Pyth API call + price computation
- `fetchHistoricalSOLPrice` now delegates to pure function and caches non-null results

### Tests
- 12 new tests in `src/__tests__/utils/dataFetching.test.ts`
- Tests for all 3 pure functions: success, HTTP errors, empty data, edge cases
- Full suite: 203 tests passing across 9 files
- tsc clean, lint clean, build passes

### Design decision
Followed Option 5: pure fetch functions are the primary testable units. Cached wrappers are ~3-5 line compositions that don't need their own tests (CacheManager's getOrFetch is already well-tested). No DI framework needed.
