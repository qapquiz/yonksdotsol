{
"id": "1f647c7b",
"title": "Deepen Position Value Computation (calculations.ts + formatters.ts)",
"tags": [
"architecture",
"deepening"
],
"status": "closed",
"created_at": "2026-04-24T19:12:18.444Z"
}

## Completed — Phase 1 + Phase 2

### Phase 1 — Extract `calculateTokenPairUSD` core ✅

- Created `calculateTokenPairUSD(xRaw, yRaw, tokenX, tokenY) → number` as the single BigInt→USD conversion function
- All 4 public functions now delegate to it (signatures unchanged for backwards compat)
- Added 6 focused tests for the core function

### Phase 2 — Consolidate `formatUPNLDisplay` ✅

- Removed `formatUPNLDisplay` from `calculations.ts`
- Single source of truth: `formatters.ts` (nullable version)
- `PositionHeader` now imports both `formatUPNLDisplay` and `formatUPNLDisplaySol` from `formatters.ts`
- Added `formatUSD(value: number) → string` to formatters.ts for the formatting concern
- All 101 tests pass, tsc clean, build passes

### Phase 3 — Deferred (collapse wrappers at call sites)

- The 3 string-returning wrappers (`calculatePositionTotalValue`, `calculateUnrealizedFeesValue`, `calculateClaimedFeesValue`) remain as thin one-liners
- Can be collapsed when we're already editing consumer files

### Results

- calculations.ts: 304 → 256 lines (−48)
- formatters.ts: 55 → 62 lines (+7 for formatUSD)
- Net: −41 lines, 0 duplication, cleaner module boundary
